name: example
description: "Example web app demonstrating the onion_device_preview package."
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  onion_device_preview:
    path: ../
  file_picker: ^8.1.2
  desktop_drop: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
